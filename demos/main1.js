// let person = { first: "Shane", last: "Holman" };

// function Greeter({ first, last }) {
// 	//props.first = "Dave";		 cannot update props, they are read only.

// 	// const first = props.first;
// 	// const last = props.last;   	can combine these into one line of code and remove props. from h1 tag in return.

// 	//const { first, last } = props; 	further, you can remove this and palce the prop names in the function parenthesis
// 	return (
// 		<h1>
// 			Hello {first} {last}
// 		</h1>
// 	);
// }
// ReactDOM.createRoot(document.getElementById("root")).render(<Greeter first={person.first} last={person.last} />);

// //react is doing the below when react is called
// //let properties = {first: Eric}
// //Greeter(properties);
